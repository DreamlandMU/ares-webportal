import Component from '@ember/component';
import { get, set, computed } from '@ember/object';

export default Component.extend({
//    currentDamage: get(char.fs3.damage).filter(wound => !wound.severity.includes('Healed')),
});
